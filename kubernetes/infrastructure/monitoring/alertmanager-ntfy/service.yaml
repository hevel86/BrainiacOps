# kubernetes/infrastructure/monitoring/alertmanager-ntfy/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: alertmanager-ntfy
  namespace: monitoring
spec:
  selector:
    app: alertmanager-ntfy
  ports:
  - port: 8080
    targetPort: 8080
    name: http
