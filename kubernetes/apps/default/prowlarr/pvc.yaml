# --- CONFIG PVC (Longhorn) ---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-config-lh
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
  volumeMode: Filesystem