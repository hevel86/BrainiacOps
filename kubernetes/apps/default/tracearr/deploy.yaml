apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracearr
  namespace: default
  labels:
    app: tracearr
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: tracearr
  template:
    metadata:
      labels:
        app: tracearr
    spec:
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
      containers:
        - name: tracearr
          image: ghcr.io/connorgallopo/tracearr:2.2.3
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: NODE_ENV
              value: production
            - name: HOST
              value: 0.0.0.0
            - name: PORT
              value: "3000"
            - name: TZ
              value: America/New_York
            - name: TRACEARR_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tracearr-secrets
                  key: TRACEARR_DB_PASSWORD
            - name: DATABASE_URL
              value: postgres://tracearr:$(TRACEARR_DB_PASSWORD)@tracearr-db:5432/tracearr
            - name: REDIS_URL
              value: redis://tracearr-redis:6379
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: tracearr-secrets
                  key: TRACEARR_JWT_SECRET
            - name: COOKIE_SECRET
              valueFrom:
                secretKeyRef:
                  name: tracearr-secrets
                  key: TRACEARR_COOKIE_SECRET
            - name: CORS_ORIGIN
              value: "*"
            - name: LOG_LEVEL
              value: info
          volumeMounts:
            - name: backups
              mountPath: /data/backup
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
      volumes:
        - name: backups
          persistentVolumeClaim:
            claimName: tracearr-backups-pvc
