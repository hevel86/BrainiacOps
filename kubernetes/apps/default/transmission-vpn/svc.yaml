apiVersion: v1
kind: Service
metadata:
  name: transmission-vpn
  namespace: default
spec:
  selector:
    app: transmission-vpn
  ports:
    - name: web
      protocol: TCP
      port: 9091
      targetPort: 9091
  type: ClusterIP
