# kubernetes/apps/default/tautulli/svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: tautulli
  namespace: default
  labels:
    app: tautulli
spec:
  type: NodePort
  selector:
    app: tautulli
  ports:
    - name: http
      port: 8181
      targetPort: 8181
      nodePort: 30181
