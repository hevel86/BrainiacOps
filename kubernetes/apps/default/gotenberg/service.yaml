apiVersion: v1
kind: Service
metadata:
  name: gotenberg
  namespace: default
spec:
  selector:
    app: gotenberg
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
