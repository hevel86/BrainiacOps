# --- CONFIG PVC (Longhorn) ---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: komga-config-lh
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn
  volumeMode: Filesystem