---
# --- SERVICE (MetalLB) ---
apiVersion: v1
kind: Service
metadata:
  name: komga
  namespace: default
spec:
  type: LoadBalancer
  loadBalancerIP: 10.0.0.205
  selector:
    app: komga
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
