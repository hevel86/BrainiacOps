apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: semaphoreui
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "30"
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  source:
    repoURL: https://github.com/hevel86/BrainiacOps
    targetRevision: HEAD
    path: kubernetes/apps/default/semaphoreui
  ignoreDifferences:
    - group: ""
      kind: Service
      name: brainiac-hostinger
      namespace: default
      jsonPointers:
        - /spec/externalName
    - group: ""
      kind: Service
      name: printer-pi
      namespace: default
      jsonPointers:
        - /spec/externalName
  syncPolicy:
    syncOptions:
      - RespectIgnoreDifferences=true
      - ServerSideApply=true
