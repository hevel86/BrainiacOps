apiVersion: v1
kind: Service
metadata:
  name: homeassistant
  namespace: default
spec:
  # No selector: endpoints are provided manually via EndpointSlice
  ports:
    - name: http
      port: 8123
      protocol: TCP
