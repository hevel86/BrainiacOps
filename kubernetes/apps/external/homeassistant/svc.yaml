apiVersion: v1
kind: Service
metadata:
  name: homeassistant
  namespace: default
spec:
  # No selector: we supply endpoints via EndpointSlice
  selector: {}
  ports:
    - name: http
      port: 8123
      protocol: TCP
